import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.newsWebLive',
  appName: 'noticias',
  webDir: 'www',
 /* server: {
    url: 'http://192.168.1.162:8100',  // Cambia "X" por el último número de tu IP local
    cleartext: true
  }*/
};

export default config;
